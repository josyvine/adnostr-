<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme (DayNight allows automatic Dark Mode switching) -->
    <style name="Theme.CloudNest" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@android:color/white</item>
        
        <!-- Secondary brand colors (Used for FAB and Toggles) -->
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorSecondaryVariant">@color/colorAccent</item>
        <item name="colorOnSecondary">@android:color/black</item>
        
        <!-- Status bar color -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        
        <!-- Surface and Background colors - Required for Material Components stability -->
        <item name="android:colorBackground">#F5F5F5</item>
        <item name="colorOnBackground">#000000</item>
        <item name="colorSurface">#FFFFFF</item>
        <item name="colorOnSurface">#000000</item>
        <item name="colorError">#B00020</item>
        <item name="colorOnError">#FFFFFF</item>

        <!-- Standard Text Colors: Ensures visibility across the app -->
        <item name="android:textColorPrimary">#000000</item>
        <item name="android:textColorSecondary">@android:color/darker_gray</item>

        <!-- Toolbar Icon & Menu Colors: Fixes the white-on-white arrow glitch -->
        <item name="colorControlNormal">@android:color/white</item>
        <item name="actionMenuTextColor">@android:color/white</item>
    </style>

    <!-- Theme for Splash Screen (No Action Bar) -->
    <style name="Theme.CloudNest.NoActionBar" parent="Theme.CloudNest">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Overlay for the Toolbar to ensure icons are readable -->
    <style name="Theme.CloudNest.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <!-- Overlay for the Toolbar Popup Menu -->
    <style name="Theme.CloudNest.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>