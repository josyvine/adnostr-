<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#121212">

    <!-- TOP NAVIGATION & ACTION BAR -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/clEditorHeader"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="#1E1E1E"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/btnCancelEditor"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:padding="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Cancel"
            android:src="@drawable/ic_rt_close"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="#FFFFFF" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Rich Text Editor"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/btnSaveRichText"
            app:layout_constraintStart_toEndOf="@+id/btnCancelEditor"
            app:layout_constraintTop_toTopOf="parent" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnSaveRichText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="#4CAF50"
            android:text="DONE"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            app:cornerRadius="8dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- MAIN TEXT CANVAS -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintBottom_toTopOf="@+id/hsvControlPanel"
        app:layout_constraintTop_toBottomOf="@id/clEditorHeader">

        <EditText
            android:id="@+id/etRichContent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@null"
            android:gravity="top|start"
            android:hint="Start typing your professional ad description..."
            android:inputType="textMultiLine|textNoSuggestions|textLongMessage"
            android:padding="24dp"
            android:textColor="#FFFFFF"
            android:textColorHint="#444444"
            android:textSize="16sp" />

    </androidx.core.widget.NestedScrollView>

    <!-- MODERN CONTROL ASSISTANCE PANEL (Bottom Formatting Toolbar) -->
    <!-- FIXED: Redesigned as a professional scrollable ribbon with uniform Material icons. -->
    <HorizontalScrollView
        android:id="@+id/hsvControlPanel"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="#1E1E1E"
        android:scrollbars="none"
        app:layout_constraintBottom_toBottomOf="parent">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingStart="12dp"
            android:paddingEnd="12dp">

            <!-- ALIGNMENT GROUP (Professional Staggered Lines) -->
            <ImageButton
                android:id="@+id/btnAlignLeft"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="12dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Align Left"
                android:src="@drawable/ic_rt_align_left"
                app:tint="#B0B0B0" />

            <ImageButton
                android:id="@+id/btnAlignCenter"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="12dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Align Center"
                android:src="@drawable/ic_rt_align_center"
                app:tint="#B0B0B0" />

            <ImageButton
                android:id="@+id/btnAlignRight"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="12dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Align Right"
                android:src="@drawable/ic_rt_align_right"
                app:tint="#B0B0B0" />

            <View
                android:layout_width="1dp"
                android:layout_height="24dp"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="8dp"
                android:background="#333333" />

            <!-- BASIC FORMATTING (Bold, Italic, Underline) -->
            <ImageButton
                android:id="@+id/btnBold"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="12dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Bold"
                android:src="@drawable/ic_rt_bold"
                app:tint="#B0B0B0" />

            <ImageButton
                android:id="@+id/btnItalic"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="12dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Italic"
                android:src="@drawable/ic_rt_italic"
                app:tint="#B0B0B0" />

            <ImageButton
                android:id="@+id/btnUnderline"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="12dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Underline"
                android:src="@drawable/ic_rt_underline"
                app:tint="#B0B0B0" />

            <View
                android:layout_width="1dp"
                android:layout_height="24dp"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="8dp"
                android:background="#333333" />

            <!-- LISTS & HEADERS -->
            <ImageButton
                android:id="@+id/btnBulletList"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="12dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Bullet List"
                android:src="@drawable/ic_rt_bullet"
                app:tint="#B0B0B0" />

            <ImageButton
                android:id="@+id/btnSubHeading"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="12dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Heading 2"
                android:src="@drawable/ic_rt_header"
                app:tint="#2196F3" />

            <View
                android:layout_width="1dp"
                android:layout_height="24dp"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="8dp"
                android:background="#333333" />

            <!-- TYPOGRAPHY TOOLS (Launches the Bottom Sheet) -->
            <ImageButton
                android:id="@+id/btnFontSize"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="12dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Font Size"
                android:src="@drawable/ic_rt_size"
                app:tint="#B0B0B0" />

            <ImageButton
                android:id="@+id/btnFontFamily"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="12dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Font Type"
                android:src="@drawable/ic_rt_font_family"
                app:tint="#B0B0B0" />

            <ImageButton
                android:id="@+id/btnTextColor"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="12dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Text Color"
                android:src="@drawable/ic_rt_color"
                app:tint="#4CAF50" />

        </LinearLayout>
    </HorizontalScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>